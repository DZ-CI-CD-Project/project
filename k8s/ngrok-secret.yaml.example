# ngrok Secret 예시 파일
# 실제 사용 시 이 파일을 복사하여 ngrok-secret.yaml로 만들고
# 실제 인증 토큰으로 변경하세요
#
# 생성 방법:
# kubectl create secret generic ngrok-secret \
#   --from-literal=auth-token='YOUR_NGROK_AUTH_TOKEN' \
#   --namespace=jobs-app
#
# 또는 이 파일을 수정 후:
# kubectl apply -f ngrok-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: ngrok-secret
  namespace: jobs-app
type: Opaque
stringData:
  # ngrok 인증 토큰 (https://dashboard.ngrok.com/get-started/your-authtoken 에서 발급)
  auth-token: "YOUR_NGROK_AUTH_TOKEN_HERE"
# Secret을 base64로 인코딩하려면:
# echo -n 'your_ngrok_auth_token' | base64

