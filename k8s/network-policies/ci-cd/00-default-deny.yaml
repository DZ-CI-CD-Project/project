apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: ci-cd  # 실제 CI/CD 네임스페이스 이름으로 변경 필요 (예: apps-job)
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress

