apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: jobs-app
  labels:
    app: frontend
spec:
  type: ClusterIP  # 또는 LoadBalancer, Ingress 사용 가능
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: frontend
